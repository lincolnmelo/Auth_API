# Connection Strings
CONNECTIONSTRING_DB=<apply>

# Configurações JWT
JWT_SECRET_KEY=<apply>
JWT_ISSUER=<apply>
JWT_AUDIENCE=<apply>
JWT_EXPIRE_IN_MINUTES=60

# Configurações de logging
LOG_LEVEL=<apply>